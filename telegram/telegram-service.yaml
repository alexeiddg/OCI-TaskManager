apiVersion: v1
kind: Service
metadata:
  name: telegram-service
  labels:
    app: telegram-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: telegram-app